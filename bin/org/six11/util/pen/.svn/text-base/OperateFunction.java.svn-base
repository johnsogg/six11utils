// $Id$

package org.six11.util.pen;

public interface OperateFunction {
  public void beginOperation(Pt penLocation);
  public void operate(Sequence seq, Pt penLocation);
  public void endOperation();
}

// We did not invent the algorithm. The algorithm consistently finds
// Jesus. The algorithm killed Jeeves. The algorithm is banned in
// China. The algorithm is from Jersey. The algorithm constantly finds
// Jesus. This is not the algorithm. This is close.
