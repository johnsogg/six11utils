package org.six11.util.pen;

/**
 * 
 * 
 * @author Gabe Johnson <johnsogg@cmu.edu>
 */
public class LengthGraph {

//  static final Comparator<AntSegment> sortByLen = new Comparator<AntSegment>() {
//
//    public int compare(AntSegment o1, AntSegment o2) {
//      return ((Double) o1.length()).compareTo(o2.length());
//    }
//
//  };
//
//  List<AntSegment> primitives;
//
//  public LengthGraph() {
//    primitives = new ArrayList<AntSegment>();
//  }
//
//  public void add(AntSegment p) {
//    int where = Collections.binarySearch(primitives, p, sortByLen);
//    if (where < 0) {
//      where = (where + 1) * -1;
//    }
//    primitives.add(where, p);
//  }
//  
//  public void remove(AntSegment p) {
//    primitives.remove(p);
//  }
//
//  public Set<AntSegment> getSimilar(double length, double tolerance) {
//    Set<AntSegment> ret = new HashSet<AntSegment>();
//    double a = length - (tolerance / 2);
//    double b = length + (tolerance / 2);
//    int idxA = search(a);
//    int idxB = search(b);
//    for (int i = idxA; i < idxB; i++) {
//      ret.add(primitives.get(i));
//    }
//    return ret;
//  }
//
//  private int search(double target) {
//    return search(0, primitives.size(), target);
//  }
//
//  private int search(int low, int high, double target) {
//    int mid = (low + high) / 2;
//    double thisLength = primitives.get(mid).length();
//    if (thisLength == target) {
//      return mid;
//    } else {
//      if (high - low == 1) {
//        if (primitives.get(low).length() > target) {
//          return low;
//        } else {
//          return high;
//        }
//      }
//      if (target < thisLength) {
//        return search(low, mid, target);
//      } else {
//        return search(mid, high, target);
//      }
//    }
//  }

}
